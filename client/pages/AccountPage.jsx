const React = require('react');

/**
 * Renders the account management page.
 * @param {object} props - Unused.
 */
function PostFeedPage(props) {
    return (
        <p>{`This page is "/account" (the account management page).`}</p>
    );
}

module.exports = PostFeedPage;
